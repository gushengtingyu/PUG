// Layout for UI elements (Tracks, Boxes, etc.)
// Map space coordinates are loaded from data.js (spaces.csv) at runtime.

var layout = {
	// UI Tracks and Off-map boxes will go here.
	// Example:
	// "Turn Track": [x, y, w, h],
	// "Discard Pile": [x, y, w, h],
}

if (typeof module !== "undefined") module.exports = layout
